# Dockerfile.secure
FROM python:3.11-slim
WORKDIR /app

# Install dependencies (Flask assumed)
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy application files (including a simple app.py)
COPY . . 

# Define the entry point for the test
CMD ["python", "app.py"]